#include <stdio.h>
//양수인지 음수인지 판별해주는 프로그램 2
int main() {
	int num;

	printf("숫자를 입력하세요: ");
	scanf_s("%d", &num);//입력 받기

	printf(num > 0 ? "양수입니다.\n" : (num < 0 ? "음수입니다.\n" : "0입니다.\n"));

	return 0;
	//양수인지 음수인지 판별해주는 프로그램1보다 코드가 짧음.
	//그러나, if문이 더 직관적이다.
}